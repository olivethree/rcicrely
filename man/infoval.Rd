% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infoval.R
\name{infoval}
\alias{infoval}
\title{Per-producer informational value with trial-count-matched reference}
\usage{
infoval(
  signal_matrix,
  noise_matrix,
  trial_counts,
  iter = 10000L,
  mask = NULL,
  cache_path = NULL,
  seed = NULL,
  progress = TRUE
)
}
\arguments{
\item{signal_matrix}{Pixels x participants numeric matrix of raw
masks (as returned by \code{\link[=ci_from_responses_2ifc]{ci_from_responses_2ifc()}} or
\code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}}).}

\item{noise_matrix}{Pixels x pool-size numeric matrix of noise
patterns (for 2IFC: the \code{stimuli} object returned by
\code{rcicr::generateStimuli2IFC(..., return_as_dataframe = TRUE)};
for Brief-RC: the same pool). Row count must match
\code{signal_matrix}.}

\item{trial_counts}{Named integer vector of trial counts per
producer. Names must match \code{colnames(signal_matrix)}.}

\item{iter}{Reference-distribution Monte Carlo size. Default
10000 (matches rcicr convention).}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix)}. When supplied, both observed and
reference norms are computed on the masked pixel subset.}

\item{cache_path}{Optional path to an \code{.rds} file. When set and
the file exists, the reference distributions are loaded from
it (keyed on trial count); when set but the file does not exist,
the computed distributions are written there after running. This
accelerates repeated runs on the same \verb{(noise_matrix, iter, mask, seed)} configuration. The cache stores only reference norms,
never observed data.}

\item{seed}{Optional integer; RNG state restored on exit.}

\item{progress}{Show a \code{cli} progress bar.}
}
\value{
Object of class \code{rcicrely_infoval} with fields:
\itemize{
\item \verb{$infoval}, per-producer z-scores (named numeric vector).
\item \verb{$norms}, per-producer observed Frobenius norms.
\item \verb{$reference}, named list of reference norm vectors, keyed by
trial count.
\item \verb{$ref_median}, \verb{$ref_mad}, named numeric vectors (centre / spread)
per trial count.
\item \verb{$trial_counts}, \verb{$mask}, \verb{$iter}, \verb{$n_pool}, \verb{$seed}, metadata.
}
}
\description{
Computes a z-scored informational value (infoVal) for each
producer's classification image, using a reference distribution
matched to that producer's trial count. Handles both 2IFC and
Brief-RC paradigms with a single function: the difference is
entirely in what the user passes as \code{noise_matrix}, not in how
the statistic is computed.
}
\details{
The observed statistic is the Frobenius norm of producer j's
classification image mask, optionally restricted to a logical
\code{mask} (e.g. an oval face region via \code{\link[=face_mask]{face_mask()}}):

\if{html}{\out{<div class="sourceCode">}}\preformatted{norm_j = sqrt(sum( signal_matrix[mask, j]^2 ))
}\if{html}{\out{</div>}}

The reference distribution for producer j is built by simulating
\code{iter} random masks at the same trial count \code{trial_counts[j]},
mirroring the \code{genMask()} construction exactly (Schmitz, Rougier,
& Yzerbyt, 2024):
\enumerate{
\item Sample \code{trial_counts[j]} stimulus ids uniformly from
\code{1:n_pool} with replacement.
\item Sample \code{trial_counts[j]} responses uniformly from \verb{\{-1, +1\}}.
\item Collapse \code{response} by \code{stim} via \code{mean()} (this is the
duplicate-stim rule used by Brief-RC).
\item Build the mask: \code{noise_matrix[, unique_stims] \%*\% mean_response / n_unique_stims}.
\item Apply \code{mask} (if supplied) and compute the Frobenius norm.
}

The per-producer z-score is
\code{(norm_j - median(ref)) / mad(ref)}, where the reference
distribution is the one matched to producer j's trial count.
Producers sharing a trial count share a reference; this makes the
simulation efficient when several producers have the same number
of trials.

Choice of statistic: the \strong{Frobenius norm} is the correct form
per Schmitz, Muller, & Yzerbyt (2019, comment) and the 2020
erratum. The original \code{rcicr::computeInfoVal2IFC()} already uses it
(\code{norm(matrix(target_ci[["ci"]]), "f")}); this function matches
that convention for both 2IFC and Brief-RC data.

Trial-count matching: the original
\code{rcicr::generateReferenceDistribution2IFC()} builds the reference
using the full pool size (\code{n_trials = ncol(noise_matrix)}). For
2IFC this is appropriate because every producer typically responds
to every pool item, so per-producer trial count equals the pool
size. For Brief-RC the producer's \emph{cognitive exposure} per trial is
richer than 2IFC (12 noisy faces vs 2), but only one chosen
\verb{(stim, +/-1)} is recorded per trial, so the number of mask
contributions is \code{n_trials} and is typically smaller than
\code{n_pool}. A pool-size reference is therefore a mismatch for
Brief-RC and biases infoVal downward. \code{infoval()} closes this gap
by keying the reference on the actual per-producer recorded trial
count.

What infoVal measures (and what it does not). The Frobenius norm
is a \strong{magnitude} statistic, not a target-alignment one: it
answers "is the mask larger than chance?" but not "is it pointing
at the right pattern?". Two consequences worth flagging when
interpreting per-producer z-scores:
\itemize{
\item \strong{Cross-paradigm comparisons need care.} Brief-RC and 2IFC are
placed on the same z-scale by \code{infoval()} (per-producer trial-
count-matched reference, identical mask construction), but the
\emph{cognitive} processes generating those masks differ. A producer
who benefits from Brief-RC's richer per-trial context might
produce a more accurately localized but not necessarily larger
mask, and the magnitude metric will not reward that. Treat
absolute Brief-RC vs 2IFC z-score differences with caution.
\item \strong{Stability and discriminability are different questions.}
\code{rel_split_half()} / \code{rel_icc()} ask whether the signal is
stable; \code{rel_cluster_test()} / \code{rel_dissimilarity()} ask whether
conditions are separable. These are the right complements to a
magnitude-based infoVal.
}
}
\references{
Brinkman, L., Goffin, S., van de Schoot, R., van Haren, N. E. M.,
Dotsch, R., & Aarts, H. (2019). Quantifying the informational
value of classification images. \emph{Behavior Research Methods},
51(5), 2059-2073. \doi{10.3758/s13428-019-01232-2}

Schmitz, M., Muller, D., & Yzerbyt, V. (2019). Comment on
"Quantifying the informational value of classification images": A
miscomputation of the infoVal metric. \emph{Behavior Research Methods}.
\doi{10.3758/s13428-019-01295-1}

Schmitz, M., Muller, D., & Yzerbyt, V. (2020). Erratum to:
Comment on "Quantifying the informational value of classification
images": Miscomputation of infoVal metric was a minor issue and
is now corrected. \emph{Behavior Research Methods}, 52, 1800-1801.
\doi{10.3758/s13428-020-01367-7}

Schmitz, M., Rougier, M., & Yzerbyt, V. (2024). Introducing the
brief reverse correlation: an improved tool to assess visual
representations. \emph{European Journal of Social Psychology}.
\doi{10.1002/ejsp.3100}
}
\seealso{
\code{\link[=face_mask]{face_mask()}}, \code{\link[=ci_from_responses_2ifc]{ci_from_responses_2ifc()}},
\code{\link[=ci_from_responses_briefrc]{ci_from_responses_briefrc()}}.
}
