% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/face_mask.R
\name{load_face_mask}
\alias{load_face_mask}
\title{Load an image-based face-region mask from a PNG or JPEG file}
\usage{
load_face_mask(path, threshold = 0.5, expected_dims = NULL)
}
\arguments{
\item{path}{Path to a PNG or JPEG mask image. Required to be the
same dimensions as the analysis image.}

\item{threshold}{Numeric in \verb{[0, 1]}. Pixels with luminance above
this threshold are \code{TRUE}. Default \code{0.5} (mid-grey).}

\item{expected_dims}{Optional integer \code{c(nrow, ncol)}. When set,
the function aborts if the loaded image's dimensions do not
match — useful for catching a wrong-resolution mask before it
silently corrupts a downstream computation.}
}
\value{
Logical vector of length \code{prod(img_dims)}, column-major.
}
\description{
Reads a binary mask image from disk and returns a logical vector
of length \code{prod(img_dims)} in column-major order — the format the
rest of the package expects. White / light pixels (above
\code{threshold}) become \code{TRUE}, dark pixels become \code{FALSE}. Use this
for image-based masks created with
\href{https://debruine.github.io/webmorphR/}{webmorphR::mask_oval()},
painted in GIMP, drawn with PowerPoint shapes, or any other tool
that produces a binary PNG/JPEG.

Companion to \code{\link[=face_mask]{face_mask()}} (which generates parametric masks
programmatically). Either function returns the same logical-vector
shape and either is accepted by \code{\link[=infoval]{infoval()}}'s \code{mask} argument or
by row-subsetting on a signal matrix.
}
\examples{
\dontrun{
# Hand-painted mask: white = include, black = exclude
fm <- load_face_mask("masks/oval_256.png", expected_dims = c(256L, 256L))
iv <- infoval(signal_matrix, noise_matrix, trial_counts,
              mask = fm, iter = 1000L, seed = 1L)
}
}
\seealso{
\code{\link[=face_mask]{face_mask()}}, \code{\link[=infoval]{infoval()}}.
}
