% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rel_split_half.R
\name{rel_split_half_null}
\alias{rel_split_half_null}
\title{Compute only the empirical-null distribution for split-half}
\usage{
rel_split_half_null(
  n_participants,
  n_pixels,
  null = c("permutation", "random_responders"),
  noise_matrix = NULL,
  n_permutations = 2000L,
  seed = NULL,
  pid = NULL
)
}
\arguments{
\item{n_participants}{Integer. Number of producers per side of
the split. Should match the observed analysis's
\code{ncol(signal_matrix)}.}

\item{n_pixels}{Integer. Number of pixels in the signal vector
(used by \code{null = "permutation"} to size the random columns).}

\item{null}{Either \code{"permutation"} or \code{"random_responders"}.}

\item{noise_matrix}{Required for \code{"random_responders"}. Pixels x
pool-size numeric matrix.}

\item{n_permutations}{Integer. Default 2000.}

\item{seed}{Optional integer.}

\item{pid}{Optional internal progress-bar id (used when called
from inside \code{rel_split_half()}); end users should leave at
\code{NULL}.}
}
\value{
Numeric vector of length \code{n_permutations} containing
per-iteration \code{r_hh} values under the chosen null.
}
\description{
Standalone access to the null-distribution simulation used by
\code{rel_split_half(null = ...)}. Useful when the user wants to
precompute and reuse a null across multiple observed analyses
(e.g., when comparing across conditions with the same producer
count).
}
