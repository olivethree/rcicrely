% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_between.R
\name{run_between}
\alias{run_between}
\title{Run every between-condition discriminability metric}
\usage{
run_between(
  signal_matrix_a,
  signal_matrix_b,
  img_dims = NULL,
  n_permutations = 2000L,
  n_boot = 2000L,
  cluster_threshold = 2,
  alpha = 0.05,
  ci_level = 0.95,
  mask = NULL,
  seed = NULL,
  progress = TRUE,
  acknowledge_scaling = FALSE
)
}
\arguments{
\item{signal_matrix_a, signal_matrix_b}{Pixels x participants,
base-subtracted. Row counts must match.}

\item{img_dims}{Integer \code{c(nrow, ncol)}. If \code{NULL}, inferred from
the \code{img_dims} attribute on \code{signal_matrix_a}, or from
\code{sqrt(n_pixels)} if that's a whole number.}

\item{n_permutations}{Passed to \code{\link[=rel_cluster_test]{rel_cluster_test()}}. Default 2000.}

\item{n_boot}{Passed to \code{\link[=rel_dissimilarity]{rel_dissimilarity()}}. Default 2000.}

\item{cluster_threshold}{Passed to \code{\link[=rel_cluster_test]{rel_cluster_test()}}. Default 2.0.}

\item{alpha}{Passed to \code{\link[=rel_cluster_test]{rel_cluster_test()}}. Default 0.05.}

\item{ci_level}{Passed to \code{\link[=rel_dissimilarity]{rel_dissimilarity()}}. Default 0.95.}

\item{mask}{Optional logical vector of length
\code{nrow(signal_matrix_a)}. Threaded through to both
\code{rel_cluster_test()} (zero-out pattern, preserving 2D structure)
and \code{rel_dissimilarity()} (drop pattern, both matrices subsetted
identically). See \code{vignette("tutorial")} §6.6.}

\item{seed}{Optional integer.}

\item{progress}{Show \code{cli} progress bars.}

\item{acknowledge_scaling}{Logical. Forwarded to both
\code{\link[=rel_cluster_test]{rel_cluster_test()}} and \code{\link[=rel_dissimilarity]{rel_dissimilarity()}}; when \code{FALSE}
(default), known-rendered inputs error.}
}
\value{
Object of class \code{rcicrely_report} with \verb{$results} =
named list of two \verb{rcicrely_*} objects (\code{cluster_test},
\code{dissimilarity}) and \verb{$method = "between"}.
}
\description{
Convenience orchestrator that runs \code{\link[=rel_cluster_test]{rel_cluster_test()}} and
\code{\link[=rel_dissimilarity]{rel_dissimilarity()}} on two condition signal matrices and wraps
both results in an \code{rcicrely_report} for joint printing / plotting.

Use this when you want both the spatial-pattern test and the
overall magnitude test in one call.
}
\section{Reading the result}{

\verb{$results$cluster_test} and \verb{$results$dissimilarity}, one result
object each, fields as in the standalone functions.
\verb{$method = "between"}.
}

\section{Reliability metrics expect raw masks}{

Both downstream metrics are scale-sensitive: the cluster test
uses variance-based Welch t, and Euclidean distance in
\code{rel_dissimilarity()} is sensitive to any scaling. See
\code{vignette("tutorial", package = "rcicrely")} chapter 3.
}

\seealso{
\code{\link[=rel_cluster_test]{rel_cluster_test()}}, \code{\link[=rel_dissimilarity]{rel_dissimilarity()}}, \code{\link[=run_within]{run_within()}}
}
